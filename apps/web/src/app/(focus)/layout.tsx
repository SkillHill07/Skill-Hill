/**
 * Focus layout — distraction-free coding experience.
 * No navbar, no footer. Used for contest workspaces and practice problems.
 */
export default function FocusLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
